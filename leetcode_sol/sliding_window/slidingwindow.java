public class SlidingWindow {
    public static void main(String[] arg) {

    //Write a function to calculate max num of fruits from int fruit[n]
    
    
    //have two pointers acting as start, and end
    // move pointer, add/subtract new element if new max > current max and in subarray, 
    // add new element to subarray
    //else move start
    //bounds n elements <-=2
    //max number
    //data shapes: 
    //[ 0 1 2 2 3 4 5]

    //Improvement 1:
    // HashMap: 
    }
}